import { invalid_sum } from "../testing_js/invalid_sum";

test( 
    "invalid_sum function is supposed to compute difference of the two arguments passed to it", 
    () => { 
        const result = invalid_sum(7, 10);
        // Asseting result to be 17
       // expect(result).toBe(17);
    }
);
